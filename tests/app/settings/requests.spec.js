// var chai = require('chai'),
//      expect = chai.expect,
//      requests = require('../../../app/settings/requests');
//
// describe.only('app/settings/requests.js:', function () {
//      it('request types exist and return promises of true', function (done) {
//           var user = {};
//           var request = {
//                type: 'link'
//           };
//           var permissions = {
//
//           };
//           requests.validate(user, request, permissions).then(function (e) {
//                expect(e).to.equal(true);
//           });
//           var request = {
//                type: 'capture'
//           };
//           requests.validate(user, request, permissions).then(function (e) {
//                expect(e).to.equal(true);
//                done();
//           });
//           var request = {
//                type: 'fakeType'
//           };
//           requests.validate(user, request, permissions).then(function (e) {
//                expect(e).to.equal(true);
//                done();
//           });
//      });
// });
//
// describe.only('app/settings/requests.js:', function () {
//      it('request types exist and return promises of true', function (done) {
//           var user = {};
//           var request = {
//                type: 'link'
//           };
//           var permissions = {
//
//           };
//           requests._preFlight(req,res,needs,function (e) {
//             console.log('callback',e);
//                expect(e).to.equal(true);
//           });
//      });
// });
